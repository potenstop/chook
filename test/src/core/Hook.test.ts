import { expect } from "chai";
import * as Hook from "../../../src/core/Hook";
describe("测试 hook", () => {
    it("main()", async () => {
        const hookLinkHead = Hook.HookLog.getHead();
        const a = 1;
    });
});
